{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug Tauri App",
            "type": "lldb",
            "request": "launch",
            // Nutze ${workspaceFolder} als Basis, um Fehler durch absolute Pfade zu vermeiden
            "program": "${workspaceFolder}/ets2-tool/src-tauri/target/release/ets2-tool.exe",
            "args": [],
            "cwd": "${workspaceFolder}",
            "stopOnEntry": false
        }
    ]
}
